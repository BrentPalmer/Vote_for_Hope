.container
  .row
    .col-xs-12
      %h4
        Hello: 
        = current_user.username
      %h1
        All Charities
      - @charities.each do |charity|
        %a{ href: charity_path(charity)}
          = charity.name
